#include <stdio.h>
/**
 * main - pritns all single digit numbers of base 10
 *
 * Return: Always 0(succes)
 */
int main(void)
{
	int cont;

	for (cont = 48; cont  <= 57 ; cont++)
	{
		putchar(cont);
	}
	putchar('\n');
	return (0);
}
